using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CodeTask : MonoBehaviour
{

    public int num;

    
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(Convert.ToString(num));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
